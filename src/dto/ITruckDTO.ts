
export default interface ITruckDTO {
  truckId: string;
  licencePlate: string,
  tare: number,
  capacity:number,
  maxBateryCapacity:number,
  autonomyFullChargeLoad:number,
  timeCharging: number
}
